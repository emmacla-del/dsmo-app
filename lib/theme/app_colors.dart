// lib/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const deepEmerald   = Color(0xFF0A6640);
  static const lightEmerald  = Color(0xFF2FA89F);
  static const richGold      = Color(0xFFC9920A);
  static const sunsetOrange  = Color(0xFFE8500A);
  static const deepSapphire  = Color(0xFF1A3A6E);
  static const champagne     = Color(0xFFF5E6C8);
  static const whatsappGreen = Color(0xFF075E54);

  // Light mode surfaces
  static const pageBackground = Color(0xFFFAFAF7);
  static const cardWhite      = Color(0xFFFFFFFF);
  static const warmIvory      = Color(0xFFFAFAF7);

  // Dark mode surfaces
  static const darkBackground = Color(0xFF0D0D0D);
  static const darkCard       = Color(0xFF1A1A1A);
  static const darkSurface    = Color(0xFF242424);

  // Text — light mode
  static const obsidian = Color(0xFF0D0D0D);
  static const slate    = Color(0xFF4A4A4A);
  static const silver   = Color(0xFF8A8A8A);
  static const ghost    = Color(0xFFC4C4C4);

  // Text — dark mode
  static const snowWhite  = Color(0xFFF5F5F5);
  static const lightSlate = Color(0xFFB0B0B0);
  static const darkSilver = Color(0xFF6A6A6A);
}
