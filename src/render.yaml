services:
  - type: web
    name: dsmo-backend
    runtime: node
    buildCommand: npm install && npm run build && npm run prisma:migrate:deploy
    startCommand: npm run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: PORT
        value: 3000